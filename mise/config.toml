[tools]
bun = "latest"
node = "24"
pnpm = "latest"
# copilot-api / claude-code-router は mise の npm: backend だと
# better-sqlite3 のネイティブビルドスクリプトが新しいnpmのスクリプト
# ブロックポリシーに引っかかり動かないため、従来通りansibleの
# community.general.npm(ホストのnpm経由)で管理する。
"npm:difit" = "latest"
